/* --------------------------------------------------------------------------
   main.js

   Entry point. Everything above has only defined things; this starts the app.

   Uses:
     storage.js            boot
   -------------------------------------------------------------------------- */
"use strict";

boot();
